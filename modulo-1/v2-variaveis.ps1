<#

V2-VARIAVEIS.PS1

Vídeo 2 – Variáveis, tipos e operadores
    Objetivo: Ensinar lógica básica de programação em PowerShell.
    Roteiro: Variáveis ($nome, $idade = 25); Tipos (int, string, bool, arrays, hashtables); Operadores (-eq, -like, -gt, -and, -or). 
    Exemplo: validar se idade > 18 e mostrar mensagem.

#>